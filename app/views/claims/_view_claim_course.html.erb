<table align="center" class="table table-striped" >
<tbody>
<thead>
</thead>
 <tbody class="product-index">
      <%= batches_present%>
      <% @batches.each do |batch| %>
        <tr>
          <td><%= link_to batch.full_name, show_claim_claim_path(batch)%>
        </tr>
      <% end %>   
  </tbody>
</table>
