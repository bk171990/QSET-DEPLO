<!DOCTYPE html>
<html>
  <head>
    <style type="text/css">
      #heading {
        width:100%;
        margin: 0 auto;
      }
      #head {
        height: 100px;
        width: 700px;
        text-align: center;
        font-size: 40px;
        border-color: black;
      }
      #head_info {
        text-align: center;
        font-size: 15px;
      }

    </style>
  </head>
  <body>
    <div id="heading">
      <div id="head">
        <b><u><%= @general_setting.school_or_college_name%></u></b>
      </div>  
      <div id="head_info">
          <u><%= @general_setting.school_or_college_address%></u><br/>
          <u><%= @general_setting.school_or_college_phone_no%></u>
      </div>
    </div>  
      <hr>
      <br/><h3><center><u>Fine Collections Report</u></center></h3><br/>
      <table cellpadding="5" cellspacing="5" width="700">
        <tr bgcolor="99CCFF">
          <th>Sl no</th>
          <th>Batch and Course</th>
          <th>Student Name</th>
          <th>Admission No</th>
          <th>Book Title</th>
          <th>Book No</th>
          <th>Total Fine</th> 
        </tr>
        <%total_amount=0%>
        <% @fines.each_with_index do |fine,i|%>
            <tr>
             <td><%=i+1%></td>
             <td><%= fine.student.batch.course.course_name.capitalize+" "+fine.student.batch.course.section_name.capitalize+" "+fine.student.batch.name.capitalize %></td>
             <td><%= fine.student.first_name+" "+fine.student.last_name%></td>
             <td><%= fine.student.admission_no %></td>
             <td><%= fine.book.title %></td>
             <td><%= fine.book.book_number %></td>
             <td><%= fine.amount%></td>
            </tr>
            
            <% total_amount+= fine.amount.to_f%>
           <%end%>
        <tr align="right">
          <th></th>
          <th></th>
          <th></th>
          <th></th>
          <th></th>
          <th></th>
          <th><b>Total:<%= total_amount%></b></th>
        </tr>
      </table>
      <br/><br/>
    </div>
  </body>
</html>
