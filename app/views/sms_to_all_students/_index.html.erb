<h3>Total Sms Sent :<%= @sms_to_all_students.count %></h3>
<div class="row">
  <div class="box col-md-12">
    <div class="box-inner">
      <div class="box-header well"></div>
      <div class="box-content" style="overflow-y: auto;height: 550px;width:100%">
        <table id="datatable" class="table table-striped table-bordered responsive">
            <thead>
              <th>Sl no.</th>
              <th>Message Body</th>
              <th>Student Name</th>
              <th>Contact</th>
              <th></th>
            </thead>
            <tbody>
              <% @sms_to_all_students.each_with_index do |message,i| %>
                <tr>
                  <td><%= i+1 %></td>
                  <td><%= message.body %></td>
                  <td><%= message.student.first_name+ " " +message.student.last_name%></td>
                  <td><%= message.student.phone1 %></td>
                </tr>
            </tbody>
          <%end%>
        </table>
      </div>
    </div>
  </div>
</div>

